# Repository list for scripts.extract_git_stats — one "org/repo" per line.
# Blank lines and lines starting with '#' are ignored.
#
# This file (when present and non-empty) overrides the script's built-in
# default list. Override priority: --repos CLI flag > HMND_GIT_REPOS env >
# this file > built-in default (HumanoidTeam/hmnd + hmnd-cloud + hmnd-sim).
#
# Synced with local-clone set for 1-to-1 audit consistency.
# To re-sync local clones to match this list, run on MacBook/dev machine:
#   bash scripts/clone_local_repos.sh
#
# Re-run on the VM after editing:
#   docker compose exec dashboard python -m scripts.extract_git_stats
#   docker compose exec dashboard python -m scripts.sync --days 90
#   docker compose kill dashboard && docker compose up -d dashboard

# ── Core monorepos (3 — reverted to default scope 2026-05-16) ──
HumanoidTeam/hmnd
HumanoidTeam/hmnd-cloud
HumanoidTeam/hmnd-sim

# History: tried 12-repo expansion with 9 active firmware/drivers, rolled back
# because firmware repos are too low-signal for productivity metrics (typical
# 3-50 commits per repo, dominated by Acontis / vendor refactors). Core 3
# captures ~95%+ of first-party engineering output.
#
# To re-expand: uncomment lines below + run scripts/extract_git_stats AND
# delete this notice. To clean up DB after removing repos: run
# scripts/cleanup_removed_repos.py.
#
#HumanoidTeam/hm-ops
#HumanoidTeam/Gripper_Firmware
#HumanoidTeam/firmware_hal_aurix_tc3
#HumanoidTeam/firmware_hal_arm_stm32h7
#HumanoidTeam/firmware_hal_c2000ware
#HumanoidTeam/etherlab-ethercat
#HumanoidTeam/manus_ros_drivers
#HumanoidTeam/ros2_xhand
#HumanoidTeam/beta-morphology-analysis-tools

# Do NOT add: vendored forks (placo, IsaacLab, lerobot_lib, robocasa,
# BehaviorTree.ROS2, ruckig, ros-noetic, etc.) — changes there belong
# upstream, not in HMND productivity metrics.

